// This route file intentionally re-exports the feature screen implementation.
// Keep it minimal so the route maps to the feature implementation and
// avoid duplicate code here.
export { default } from '@/features/play/friends/screens/FriendChallengeScreen';
