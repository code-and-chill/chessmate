meta {
  name: Post Reject Proposal
  type: http
  seq: 12
}

post {
  url: ${baseUrl}/tickets/${ticketId}/proposal/reject
  auth: none
}

headers {
  Authorization: Bearer ${authToken}
}

tests {
  test("proposal rejection requires pending proposal", function() {
    expect([200, 409, 404]).to.include(res.status);
  });
}
