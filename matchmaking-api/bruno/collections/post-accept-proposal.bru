meta {
  name: Post Accept Proposal
  type: http
  seq: 11
}

post {
  url: ${baseUrl}/tickets/${ticketId}/proposal/accept
  auth: none
}

headers {
  Authorization: Bearer ${authToken}
}

tests {
  test("proposal acceptance requires pending proposal", function() {
    expect([200, 409, 404]).to.include(res.status);
  });
}
